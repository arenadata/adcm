/adcm/data/log/nginx/*.log {
        su root root
        size 50M
        missingok
        sharedscripts
        compress
        delaycompress
        rotate 10
        postrotate
                kill -USR1 `cat /run/nginx/nginx.pid`
        endscript
}
