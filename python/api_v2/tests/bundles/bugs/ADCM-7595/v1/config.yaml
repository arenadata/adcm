---
- type: cluster
  name: cluster_without_config
  version: &version '1.1'
  edition: community
  config_group_customization: true

  config: &config
    - name: float_field
      type: float
      required: false
      default: 0.1

- name: service_1
  type: service
  version: *version
  config: *config

  components:
    component_1:
      constraint: [ 0, + ]
      config: *config
