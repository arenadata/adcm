---

version: 1

roles:
    - name: base
      description: Base user role
      apps:
        - label: auth
          models:
            - name: user
              codenames:
                - view
                - self_change_password
