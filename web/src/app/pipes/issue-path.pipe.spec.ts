import { IssuePathPipe } from './issue-path.pipe';

describe('IssuePathPipe', () => {
  it('create an instance', () => {
    const pipe = new IssuePathPipe();
    expect(pipe).toBeTruthy();
  });
});
