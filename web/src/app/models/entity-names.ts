import { TypeName } from '../core/types';

export const EntityNames: TypeName[] = ['servicecomponent', 'group_config', 'component', 'host', 'service', 'cluster', 'provider', 'job', 'task', 'bundle'];
