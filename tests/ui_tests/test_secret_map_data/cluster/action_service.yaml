---
- hosts: all
  connection: local
  gather_facts: no

  tasks:
    - name: "Action service"
      adcm_config:
        type: "service"
        key: "secret_map"
        value: {'key_secret': 'value_secret'}
  run_once: true
  tags:
    - always