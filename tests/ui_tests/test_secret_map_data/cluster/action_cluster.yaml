---
- hosts: all
  connection: local
  gather_facts: no

  tasks:
    - name: "Action cluster"
      adcm_config:
        type: "cluster"
        key: "secret_map"
        value: {'key_secret': 'value_secret'}
  run_once: true
  tags:
    - always