---
- hosts: all
  connection: local
  gather_facts: no

  tasks:
    - name: "Action component"
      adcm_config:
        type: "component"
        key: "secret_map"
        value: {'key_secret': 'value_secret'}
  run_once: true
  tags:
    - always