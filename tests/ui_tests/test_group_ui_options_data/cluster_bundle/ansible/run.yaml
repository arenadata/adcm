---
- name: run cluster
  hosts: all
  connection: local
  gather_facts: no

  tasks:
    - name: emulation run all item of the cluster
      pause:
        seconds: 5
    - debug:
        msg: "Unstucked now"
