---
- hosts: localhost
  gather_facts: false
  connection: local
  tasks:
    - name: ADCM Check
      adcm_check:
        title: "Check"
        result: no
        msg: "Description of check or results of check. Required, if no ‘success_msg’ and ‘fail_msg’ fields"
