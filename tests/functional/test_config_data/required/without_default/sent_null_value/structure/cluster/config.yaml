---
- name: cluster_structure_required_without_default_sent_null_value
  type: cluster
  version: '1.0'
  config:
  - name: structure
    type: structure
    required: true
    yspec: ./schema.yaml
  actions:
    job:
      script: cluster_action.yaml
      script_type: ansible
      type: job
      states:
        available:
        - created
- name: service_structure_required_without_default_sent_null_value
  type: service
  version: '1.0'
  config:
  - name: structure
    type: structure
    required: true
    yspec: ./schema.yaml
  actions:
    job:
      script: service_action.yaml
      script_type: ansible
      type: job
      states:
        available:
        - created
