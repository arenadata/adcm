---
- hosts: localhost
  gather_facts: false
  connection: local
  tasks:
    - adcm_state:
        type: "cluster"
        state: "statey"
      register: out

    - name: "Check return value"
      fail:
        msg: "Expected value is statey, actual value is {{ out.state }}"
      when: out.state != "statey"
